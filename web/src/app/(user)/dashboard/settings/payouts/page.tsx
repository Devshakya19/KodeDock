import { PayoutSettings } from "@/components/settings/payouts";
export default function DashboardPayoutsSettingsPage() {
  return (
    <div className="w-full">
      <PayoutSettings />
    </div>
  );
}
