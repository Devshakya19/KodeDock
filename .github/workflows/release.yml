name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract Release Notes
        id: extract_notes
        run: |
          # Get the tag name (e.g., v1.5.0)
          TAG_NAME=${GITHUB_REF#refs/tags/}
          
          # Extract notes from CHANGE.md between current tag and the next tag/footer
          # We look for "## [$TAG_NAME]" and print until we hit the next "## [" or "---"
          awk -v tag="## [$TAG_NAME]" '
            index($0, tag) == 1 {flag=1; next}
            /^## \[/ && flag {flag=0; exit}
            /^---/ && flag {flag=0; exit}
            flag {print}
          ' CHANGE.md > release_notes.md
          
          echo "Extracted notes:"
          cat release_notes.md

      - name: Create Professional Release
        uses: softprops/action-gh-release@v2
        with:
          name: KodeDock ${{ github.ref_name }}
          body_path: release_notes.md
          draft: false
          prerelease: contains(github.ref_name, '-rc') || contains(github.ref_name, '-beta')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
